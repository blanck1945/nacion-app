/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "plus.unsplash.com",
      "images.unsplash.com",
      "bucket1.glanacion.com",
    ],
  },
};

module.exports = nextConfig;
